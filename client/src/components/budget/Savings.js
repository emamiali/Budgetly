import React, { Component } from 'react';

export default class Savings extends Component {
  render() {
    return (
      <div>
        <p>This will be the saving component. This component will take the savings from the user database and subtract it from whatever the user has saved and return the remaining</p>
      </div>
    )
  }
}
